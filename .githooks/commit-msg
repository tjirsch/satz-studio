#!/usr/bin/env bash
# Names gate on the commit MESSAGE — the content rules CI applies to every
# commit in a push, applied before the commit exists.
# Enable once per clone:  git config core.hooksPath .githooks
exec "$(git rev-parse --show-toplevel)/scripts/check-names.sh" --message "$1"

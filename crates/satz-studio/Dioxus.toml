# What `dx bundle` reads (dioxus-cli 0.7.10, `src/config/bundle.rs`: BundleConfig,
# DebianSettings, MacOsSettings, WindowsSettings). The icons are the PNGs under
# assets/icons/: one drawn symbol, rendered at 1024 px by assets/icons/render.py and
# resampled with `sips -z N N icon-1024.png --out icon-N.png`. dx builds the macOS
# .icns from the PNG list itself and installs the PNGs under /usr/share/icons/hicolor
# in the .deb and the AppImage; it derives no .ico, so the Windows executable and the
# MSI take `windows.icon_path`, which wraps the 256 px PNG (Windows reads a
# PNG-compressed icon). The MSI upgrade code is the one dx derives from the product
# name and the architecture (a UUID v5), stable as long as the package name is.
[application]
name = "satz-studio"

[bundle]
identifier = "io.github.tjirsch.satz-studio"
publisher = "tjirsch"
copyright = "2026"
category = "DeveloperTool"
short_description = "The desktop app for satz"
long_description = "satz-studio runs the satz interview, edits the estate map and the estate files through a typed interface, runs satz commands, and drives satz through Claude."
icon = [
  "assets/icons/icon-32.png",
  "assets/icons/icon-128.png",
  "assets/icons/icon-256.png",
  "assets/icons/icon-512.png",
  "assets/icons/icon-1024.png",
]
# The licence, the notice and the third-party licence texts every bundle carries, as
# Apache 2.0 §4(a) and §4(d), MIT and the other crate licences ask of a redistribution:
# the app's LICENSE and NOTICE, the crates' texts (THIRD-PARTY-LICENSES.md, generated by
# scripts/update-third-party-licenses.sh), and the two texts NOTICE names that are no
# crate — the Material Symbols font's and the vendored grammar's. dx copies them into
# SatzStudio.app/Contents/Resources/ (the .dmg holds that .app), into the MSI's install
# directory beside the executable, and into /usr/lib/SatzStudio/ in the .deb and the
# AppImage. dx resolves `resources` against the directory it runs in, not against this
# crate as it does `icon`, and skips a path that is not there with a warning:
# `dx bundle --package satz-studio` runs from the repository root, as release.yml and
# CONTRIBUTING.md run it. dx copies each file under its file name alone, into one
# directory, so no two of them may share a name — hence LICENSE-MaterialSymbols and
# LICENSE-satz-tree-sitter.
resources = [
  "LICENSE",
  "NOTICE",
  "THIRD-PARTY-LICENSES.md",
  "crates/satz-studio/assets/fonts/LICENSE-MaterialSymbols",
  "vendor/satz-tree-sitter/LICENSE-satz-tree-sitter",
]

# the runtime halves of the -dev packages ci.yml installs (Ubuntu 24.04 names its
# time64 builds `…t64` and declares the plain name as provided)
[bundle.deb]
depends = ["libwebkit2gtk-4.1-0", "libgtk-3-0", "libayatana-appindicator3-1", "librsvg2-2", "libxdo3"]

[bundle.windows]
icon_path = "assets/icons/icon.ico"

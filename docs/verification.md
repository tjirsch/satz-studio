# satz-studio verification

What is proven, where, and how to run it. Every automated check runs offline against
`tests/fixtures` and the pinned `vendor/satz`; the ones that drive the `satz` binary
need it installed at `MIN_SATZ` or newer. The GitHub reads — the look for a satz-studio
update, satz's installer — run against a canned server on a local port. The manual checks
that need a window or a machine are listed at the end.

## 1. What is proven where

The end-to-end tests (`crates/satz-studio-core/tests/e2e_*.rs`) take the paths the
app takes — `Snapshot::take`, then `Snapshot::delegate` around `satz_interview` over
the estate's `satz mcp` with `McpChecker`; `EditSession::apply` and `Proposed::commit`;
a pack switched through `satz_add_pack` and `satz_remove_pack` — against a
temporary estate written by `satz interview --create` over `vendor/satz`, and against
a copy of satz's smoke estate. The unit tests of each module are named where they
carry a claim the harness relies on.

| claim | test file | test |
|---|---|---|
| The seventeen day-0 answers satz's smoke matrix pipes into `satz interview` (`vendor/satz/scripts/smoke.sh`), given one `satz_interview` call per typed value and then `accept_defaults`, end complete: `summary.complete`, seventeen answered, `written == 10` for the eight defaults and the two derived names, `rename_to == "C0example.satz"` | `tests/e2e_interview.rs` | `the_app_path_and_the_cli_path_end_in_the_same_estate` |
| A derived name is offered once its input is typed: `infra_project_name` is `blocking` until `customer_shortname` lands, then defaults to `acme-infra-001` | `tests/e2e_interview.rs` | same |
| The `questions` report `SatzCli::json_report` reads back carries the same questions and summary as the last `satz_interview` | `tests/e2e_interview.rs` | same |
| The CLI-driven interview on a second copy, fed the same piped input, writes the same `params` set and the same `use` line states; outside `params { }` the two files are byte-identical | `tests/e2e_interview.rs` | same |
| The map switched on through `satz_add_pack` removes the `// ` of the exact line `// use "presets/estate-map.satz"` and nothing else, binds nothing, and the check passes; the model's map row reads `commented` before and `active`, deploying, after; the map's choices are open afterwards | `tests/e2e_map.rs` | `the_map_and_a_pack_are_switched_by_satz_and_a_refused_switch_writes_nothing` |
| The billing permissions switched on while no security-group model is: `satz_add_pack` refuses, naming them, and the file keeps its bytes | `tests/e2e_map.rs` | same |
| The budget pack switched on through `satz_add_pack` binds `use_budget = true` and uncomments `use "presets/organization-budget.satz" when use_budget`; the model's row is `active`, deploying, at that line, and the gate is no param row. Switched off through `satz_remove_pack`, the param reads `false` and the line stays active; the row is `active` and does not deploy | `tests/e2e_map.rs` | same |
| A gate bound `true` whose line is gone: `ReplaceParam` appends it and the commit lands at satz's default validation level; the check prints the unadopted-pack finding in satz's layout, which `parse_satz_output` turns into a diagnostic naming the pack and its `fix: satz add-pack` command; satz's pack report has the pack `absent` with its gate answered `true`, the sentence among its row's findings and the `satz add-pack` command as the `fix` of the report's `unadopted-pack` finding about it; there is no param row for the gate; switched on through `satz_add_pack` afterwards, its line is written and the row deploys with no finding | `tests/e2e_map.rs` | `a_gate_bound_true_without_its_line_is_absent_and_the_check_names_the_pack` |
| At `validation_level = "error"` the same sentence is a refusal: `CliChecker` returns it as one error diagnostic of kind `unadopted-pack`, and a commit under that checker is `Rollback::Check` with it, the file's bytes kept, no temp file left | `tests/e2e_map.rs` | same |
| With the map switched on with its requirements and the audit logsink off, `use_central_alerts = true` through `satz_interview` does not compile and satz refuses it, naming the logsink param; the file afterwards is byte for byte what it was before the call. the pinned satz binds the gate and uncomments the line before it refuses, so the delegated write restores the bytes and the refusal ends "satz refused and had changed new.satz; the file is back as it was"; a satz that refuses before writing leaves the file untouched and the refusal is satz's sentence alone — the test holds the sentence to whichever of the two happened. The billing permissions refused by `satz_add_pack` wrote nothing: the file untouched, the refusal satz's sentence with no restore sentence | `tests/e2e_map.rs` | `a_refused_answer_leaves_the_file_as_it_was_and_says_so_when_satz_had_changed_it` |
| A pack switched on through `satz_interview` returns its notice in that call's report, once: the CIS baseline's `cis_baseline_adopted`, its pack, `satz adopt <estate> --execute --import` and `severity` `error`, with `acknowledged` false; the next write returns none and the estate still owes it; `transpile --check` warns, passes, and names the param. Binding that param `true` through `satz_interview` writes it into the estate's params, `EstateDir::acknowledged` says so, and the check has nothing left to say. A day-0 estate, which uses no pack that asks for anything, returns none | `tests/e2e_notices.rs` | `a_pack_switched_on_returns_its_notice_once_and_the_binding_takes_it_away` |
| The one answer a notice takes is `true`: `false` is refused by satz, naming the param, and writes nothing | `tests/e2e_notices.rs` | `a_notice_is_acknowledged_with_true_and_with_nothing_else` |
| The window holds a notice once, adds nothing on a call that returns it again, and never holds one satz reports as acknowledged; the notice's command is the arguments after `satz` with `<estate>` filled in, and a command that is not satz's, quotes a word or carries another placeholder says why instead of being guessed at; `bootstrap` goes to the terminal and `bootstrap --dry-run` and `adopt` do not | `crates/satz-studio/src/state/estate_actions.rs`, `crates/satz-studio/src/shell/notices.rs`, `crates/satz-studio/src/views/commands.rs` | `a_notice_is_held_once_and_a_later_call_neither_drops_nor_doubles_it`, `a_notice_the_estate_has_already_acknowledged_is_never_held`, `the_estate_takes_the_place_of_the_placeholder_and_satz_is_dropped`, `a_command_the_app_will_not_run_says_why_instead_of_being_guessed_at`, `the_terminal_takes_the_three_external_commands_and_a_dry_run_stays_in_the_app` |
| The Overview counts open notices from the compile's own `notice` findings and offers the dialog only where this session is holding one | `crates/satz-studio/src/views/overview.rs` | `the_notices_row_counts_the_compiles_own_findings_and_offers_the_dialog_it_holds` |
| The Overview owes the map while satz reports its line commented out or absent, and the pack graph's findings with the first one's sentence, each pointing at Packs | `crates/satz-studio/src/views/overview.rs` | `a_map_that_is_off_one_that_is_absent_and_the_pack_findings_are_three_rows` |
| The Overview's identity card opens with Short name, Customer, Customer ID and Organisation ID read from the file's own `params { }` block, with no questions report and without the `estate_core` pack; with that pack's report each subject is still one row, the other core answers below; a param the file does not set reads "not set" whatever the pack offers, one it sets to `""` reads "empty", and before the model is built each of the four reads "not read yet" | `crates/satz-studio/src/views/overview.rs` | `the_file_s_own_params_say_whose_estate_it_is_without_a_questions_report`, `with_the_core_report_each_subject_still_shows_once`, `a_param_the_file_does_not_set_reads_not_set_and_an_unread_file_says_so`, `a_file_value_reads_as_one_line` |
| A Decisions answer is written by Accept and Enter alone — not by the field losing focus, a switch flip or a chip change — while a param field keeps blur-to-save | `crates/satz-studio/src/views/interview.rs` | `only_enter_reaches_the_card_from_its_field` |
| The Decisions card's filled button reads Accept on a question not answered, changed or not, disabled on a value that cannot be written and while a write is in flight; Next on an answered question whose value is the written one, a `oneof` on its bound option, and Accept once the value differs; Skip only on a question not answered | `crates/satz-studio/src/views/interview.rs` | `an_unanswered_question_reads_accept_changed_or_not`, `an_answered_question_reads_next_until_its_value_differs`, `an_answered_field_changed_and_changed_back_reads_next_again`, `an_empty_field_on_a_question_that_offers_nothing_cannot_be_accepted`, `a_oneof_reads_next_on_its_bound_option_and_accept_on_another`, `skip_shows_only_on_a_question_not_answered` |
| Accept moves the walk to the question after the card's before the write lands, and the reload that takes the answered question out of the walk leaves the card there — with answered questions hidden or shown, and when the write also drops the question it moved to; the last open question empties the walk; Next and Skip move without writing; Back retraces | `crates/satz-studio/src/views/interview.rs` | `accept_advances_and_the_reload_keeps_the_card_on_the_next_question`, `accept_with_answered_shown_lands_on_the_question_that_followed`, `accept_whose_write_also_drops_the_next_question_shows_the_one_after`, `accept_on_the_last_open_question_empties_the_walk`, `next_on_an_answered_question_advances`, `skip_wraps_and_back_retraces_the_questions_left` |
| `satz import --from hcl` over a `.tf` file writes `imported-hcl.satz`, a name no flag stated: the read-back finds exactly that file, it declares an estate, and the report keeps satz's "review it, then transpile and plan" sentence and the promoted block with its `file:line` | `tests/e2e_import.rs` | `the_hcl_shape_writes_an_estate_the_read_back_finds` |
| A second import over the same source is a file WRITTEN, not a directory unchanged: the read-back is a hash of each `.satz` file and not a listing | `tests/e2e_import.rs` | `a_second_import_over_the_same_file_is_still_a_file_written` |
| A raw `.tfstate` is refused by the form before anything runs and by satz when it does, in the same words: `values.root_module`, and `tofu show -json > state.json` | `tests/e2e_import.rs` | `a_raw_tfstate_is_refused_by_the_form_and_by_satz_alike` |
| The import's argv is the flags of the chosen shape and no other's — `--organization` on a state document alone, never on a live scope or Terraform HCL, and a blank one not at all; a raw `.tfstate`, a source that is not there and a live scope that is not one are refused before a child is spawned; the report splits satz's lines into sections and loses none, and the two losses satz v0.81.0 reports on an import — attributes an apply would reset, asset types Cloud Asset Inventory does not serve — are warnings with every line under them, while the dropped API vocabulary stays in the rest | `src/satz/import.rs` | `every_state_field_renders_its_own_flag_in_order`, `a_live_scope_is_the_source_and_takes_the_same_flags`, `a_shapes_options_never_leak_into_another_shapes_command_line`, `a_blank_or_whitespace_field_is_not_passed_at_all`, `a_raw_tfstate_is_refused_before_anything_runs_whatever_its_name`, `every_line_satz_printed_is_in_exactly_one_section`, `the_losses_satz_reports_are_warnings_with_the_lines_under_them` |
| A `ReplaceValue` with the node's own value commits with the file's hash unchanged and the output of `satz transpile --check` identical before and after, the banner stripped | `tests/e2e_edit.rs` | `a_no_op_edit_leaves_the_hash_and_the_check_output_unchanged` |
| An edit on `showcase.satz`'s `audit_retention_days` line replaces the value span alone: the bytes before and after the span, the `=` column, the spaces before the comment and the comment itself are the original's; the reverse edit restores the original bytes and hash | `tests/e2e_edit.rs` | `an_edit_replaces_the_value_span_alone_and_the_reverse_edit_restores_the_bytes` |
| A good edit lands through the MCP checker; a bad edit rolls back with a diagnostic naming the real file and line, the original bytes intact, no `.studio-tmp.satz` left; a file changed on disk is refused; a delegated write is verified, and a broken one restored | `tests/edit_commit.rs` | `a_good_edit_lands_through_the_mcp_checker`, `a_bad_edit_rolls_back_naming_the_real_file`, `a_file_changed_on_disk_is_refused`, `a_delegated_write_is_verified_and_a_broken_one_is_restored` |
| `Snapshot::delegate` over fake tool outcomes: a refusal after the call wrote the file restores the recorded bytes and the refusal ends "satz refused and had changed new.satz; the file is back as it was"; a refusal that wrote nothing is satz's sentence alone, the file untouched, the checker never reached; a call that ends without a result (the session closed) is compared the same way — restored and saying so when it had written, the error alone when it had not — and a file the call removed is written back; a call that landed and whose check refuses is `RolledBack` with the bytes back | `tests/edit_delegate.rs` | `a_refusal_that_had_changed_the_file_is_restored_and_says_so`, `a_refusal_that_wrote_nothing_is_satz_s_sentence_alone`, `a_call_that_ended_without_a_result_is_compared_and_restored_too`, `a_call_that_landed_and_whose_check_refuses_is_rolled_back` |
| `McpChecker` and `CliChecker` return the same verdict and, for a refusal, the same `(file, line, kind, message)` set, on six edits | `tests/edit_checkers_agree.rs` | `the_two_checkers_agree_on_six_cases` |
| A refusal at `validation_level = "error"` is the same findings through both checkers — one JSON summary, over MCP and from `transpile --check --format json` — with the `unadopted-pack` error among them naming the pack | `tests/edit_checkers_agree.rs` | `a_refusal_is_the_same_findings_through_both_checkers` |
| At satz's default level the same estate passes, and both checkers return the same summary: the same addresses, and the `unadopted-pack` warning in the same `findings` | `tests/edit_checkers_agree.rs` | `a_check_that_passes_carries_its_warnings_through_both_checkers` |
| `ReplaceParam` writes the bytes satz's own writer writes: a replacement keeps the line as the author wrote it, an append lays the params block out as `satz fmt` does, and on an aligned line both keep the `=` column | `tests/edit_bind_parity.rs` | `a_replacement_alone_keeps_the_line_as_the_author_wrote_it`, `an_append_lays_the_block_out_and_the_two_write_the_same_bytes`, `on_an_aligned_line_the_two_keep_the_column_and_write_the_same_bytes` |
| The edit proof in memory: one value's bytes change and nothing else on its line; a param is rewritten in place or appended as `bind` appends it; a node that is not a value, a raw value that breaks the structure, and two edits on one node are refused | `tests/edit_apply.rs` | all |
| Every question of every pack under `vendor/satz/presets` is answered in the shape its param is declared with, in the pack's own text, a reference followed: a list is a list of strings, a bool a bool, a number a number — as the questions report has the row, and again with nothing offered. The test reads the declaration itself, so satz's `shape` and that reading are held to each other. The sweep runs over one estate that uses every pack, through the installed satz's report and the model the app builds; the report carries every question the library declares, and the list params `access_approval_notification_emails`, `allowed_resource_locations`, `admin_port_source_ranges` and `allowed_policy_member_subjects` are among the lists found. A pin bump that brings a pack whose field would not match its declaration fails here | `tests/e2e_answer_shapes.rs` | `every_question_of_every_pack_is_answered_in_the_shape_its_param_is_declared_with` |
| On a skeleton with the access-approval pack in, `access_approval_notification_emails` offers nothing and blocks; the report declares its shape as a list, so its field is a list; one address sent as a JSON array through `satz_interview` is written as a list of one, the check passes, the model's row for it is a `List`, and the question reads answered with that array | `tests/e2e_answer_shapes.rs` | `one_address_for_a_list_param_is_written_as_a_list_of_one` |
| The shape the pack declares decides an answer's shape, and a question declared without one takes the shape of the value it offers; a map and a question with neither have no typed field. A list field starts empty, is empty until a value is added, and sends one value as a JSON array of one | `src/model/params.rs`, `crates/satz-studio/src/components/typed_field.rs` | `the_declared_shape_decides_and_an_offer_answers_for_a_param_declared_without_one`, `one_value_in_a_list_field_is_sent_as_a_list_of_one` |
| An estate directory outside every repository is `Outside` in git's own words; `init_steps` run through `git::run` make a repository that holds the estate file's directory with one commit naming the estate and the `.gitignore`d state left out; with an empty identity git refuses the commit on stderr, the run is a failure and nothing is committed; a git that cannot be run is `NoGit` and a missing directory is not mistaken for one | `tests/git_work_tree.rs`, `src/git.rs` | all |
| The Overview owes a repository while git holds the estate outside one, with `InitRepository` offered and git's words in the row; without git the row names it and offers nothing to press; inside a repository, or before the first reload has asked, it owes none | `crates/satz-studio/src/views/overview.rs` | `an_estate_outside_a_repository_owes_one_with_the_fix_offered_and_git_s_own_words`, `without_git_the_row_names_git_and_offers_nothing_to_press`, `a_repository_or_a_fact_not_yet_read_owes_nothing` |
| The repository's `NOTICE`, which every bundle carries, contains `vendor/satz/NOTICE` verbatim, so a pin bump that changes satz's notices fails until they are copied | `tests/notice.rs` | `notice_carries_the_notice_of_satz_verbatim` |
| `Cst::text()` is the file; every `.satz` under `vendor/satz` parses through the grammar and lowers through `satz_core::satz::parse` | `tests/cst_roundtrip.rs`, `src/cst/grammar.rs` | all |
| `scan_uses` finds every line of a `satz interview --create` skeleton and only the exact shape satz's `pack_line` writes | `tests/cst_uses.rs` | `the_interview_skeleton_is_scanned_line_for_line`, `only_the_exact_shape_is_a_pack_line` |
| The model's pack report is satz's: read over the session as the reload reads it (`satz_packs`), every row — role, gate, answer, default, value, line state and number, `deploys`, each requirement with `met`, `required_by`, `excludes`, notices, findings — the unmanaged lines and the findings equal what `satz packs --format json` writes for the same estate, on the skeleton as written and interviewed with the map, the audit-log archive and the central alerts on. The rows carry the dependencies no `ask_when` declares: the central alerts and Sentinel need the archive, the findings mail the central alerts, the billing permissions a security-group model, the runner grant the runner. The archive switched off while the central alerts need it is refused, naming them. A pin bump that changes the graph is checked here without being named | `tests/e2e_pack_edges.rs` | `the_skeleton_s_rows_are_the_rows_satz_packs_writes`, `an_interviewed_estate_s_rows_are_the_rows_satz_packs_writes` |
| The Packs tree as a pure function over satz's report: lines in the file's order under their phase and the packs without a line last, whatever the graph's order; a pack another needs alone is a tree and the rest stay cells; children across three levels leave their phase, which they carry as a caption, and an emptied section goes; a requirement several packs meet, and the map's, hang a card nowhere; a child that deploys while its requirement is not met warns and the trunk before it leads there; packs that need each other stay at the top where the loop closes; a card lists what is off and the requirements the tree does not draw; a finding carries the command satz names for it, and one about no row stands apart. Over the recorded report of satz's smoke estate the central alerts and Sentinel hang from the archive, the findings mail from the central alerts and the runner grant from the runner, and the billing permissions from nothing | `crates/satz-studio/src/views/map.rs` | all |
| A switch that lands says what it switched and what it opened; one that switched nothing says what satz left as it was | `crates/satz-studio/src/state/estate_actions.rs` | `a_switch_says_what_it_switched_and_what_it_opened` |
| `MIN_SATZ` is not newer than the submodule's version; an older binary is refused naming `satz self-update` | `src/satz/binary.rs` | `min_satz_is_not_newer_than_the_submodule`, `an_older_binary_is_refused_by_version` |
| The driver is permissive upward: a fake satz at the build's satz, one patch past it and one minor past it are each located, not refused, and `ahead_of_build` says `None`, `Patch` and `Minor`; a moved major reads as a minor, build metadata on the build's release as that release. The versions derive from the vendored satz, so the tests hold across pin moves; the test on the installed satz asserts `MIN_SATZ` or newer and nothing tighter, because CI installs the newest satz | `tests/satz_binary.rs`, `src/satz/binary.rs` | `a_fake_at_the_pin_or_newer_is_located_and_says_how_far_ahead_it_is`, `the_installed_satz_is_found_at_the_pinned_version_or_newer`, `a_newer_satz_passes_the_gate_and_says_by_which_kind_of_release`, `build_metadata_on_the_pinned_release_is_that_release` |
| A newer satz is a notice, not a gate, over fake binaries through `locate` as the app coroutine runs it: the build's satz opens estates with no notice; an older satz is the one refusal; a newer patch and a newer minor open estates and give a notice each, worded by satz's release rule; a dismissed notice stays gone for that version and comes back for the next; `dismissed_satz` round-trips through the settings file | `crates/satz-studio/src/state/pace.rs` | `the_pinned_satz_opens_estates_with_no_notice`, `an_older_satz_is_the_one_refusal`, `a_newer_patch_opens_estates_and_is_told_as_a_patch`, `a_newer_minor_opens_estates_and_is_told_as_a_minor`, `a_dismissed_notice_stays_gone_for_that_version_only`, `the_dismissed_version_round_trips_through_the_settings_file` |
| The satz-studio release look, against a canned local server: it reads `releases/latest` and nothing else; a newer tag is `Available` with its page; the running release, or a build newer than the latest, is `Latest`; a 403 is the rate limit with its reset; a port nothing listens on is `Unreachable`, saying offline; a 404 is no release, and a tag that is not a version is refused. The window's sentence for each of the three outcomes, and what the top bar offers | `tests/releases.rs`, `crates/satz-studio/src/state/pace.rs` | `a_newer_release_is_named_with_its_page_and_the_latest_is_read_never_a_tag`, `the_running_release_or_a_newer_build_is_the_latest`, `a_403_is_the_rate_limit_and_says_so`, `nothing_listening_is_unreachable_and_says_offline`, `no_release_and_a_tag_that_is_not_a_version_are_said_as_such`, `the_studio_look_says_which_of_three_things_it_found` |
| The satz release check reads `satz self-update --check-only` as satz prints it: `Latest version:` against the satz asked, with the `Release:` page; the latest, or a satz newer than it, is `Latest`; an output without the line is an error quoting it. `self_update_frequency = "never"` in the operator's satz config is the one no, a missing file or key is yes, a file that does not parse is an error naming it. The top bar and the title offer a found release only while it is newer than the satz in use; the title carries the version and each available release | `src/satz/self_update.rs`, `crates/satz-studio/src/state/pace.rs` | `a_newer_release_is_read_with_its_page`, `the_latest_release_or_an_older_one_is_latest`, `an_output_without_the_line_is_an_error_naming_what_satz_printed`, `never_in_the_operators_satz_config_is_the_one_no`, `the_satz_check_offers_a_release_only_while_it_is_newer_than_the_satz_that_runs`, `the_title_carries_the_version_and_what_is_available` |
| satz's installer, against a canned local server and fake scripts: for the shell and the PowerShell installer alike, the installer and its sidecar come from the one `releases/latest` object, a script that does not match its sidecar is refused and never runs, a release without a sidecar is refused before anything is downloaded, and a sidecar that is not a SHA-256 is refused; `sh` runs the shell script and `powershell -NoProfile -NonInteractive -ExecutionPolicy Bypass -File` the PowerShell one. This system's installer — a fake `.sh` off Windows, a fake `.ps1` on Windows — runs with `SATZ_INSTALL_DIR` naming the folder, `SATZ_NO_MODIFY_PATH=1` and stdin closed. The home lookup names `satz.exe` on Windows. An installer that exits cleanly is an install only if satz is then found, and an update by the installer is done only if the satz then located is the release it installed; the installer is offered unless Settings name a satz path | `tests/releases.rs`, `src/satz/install.rs`, `src/satz/binary.rs`, `crates/satz-studio/src/state/install.rs`, `crates/satz-studio/src/state/pace.rs` | `each_installer_and_its_sidecar_come_from_the_one_latest_release`, `an_installer_that_does_not_match_its_sidecar_is_refused_and_never_runs`, `a_release_without_a_sidecar_is_refused_before_anything_is_downloaded`, `a_sidecar_that_is_not_a_sha256_is_refused`, `this_system_s_installer_runs_into_the_named_folder_without_a_path_edit_or_a_prompt`, `the_sidecar_is_read_as_sha256sum_writes_it_or_as_a_bare_hash`, `a_script_that_matches_its_sidecar_is_verified_and_one_that_does_not_is_refused`, `each_system_fetches_its_own_installer_and_sidecar`, `the_shell_script_runs_under_sh_and_the_powershell_one_past_the_execution_policy`, `the_home_binary_carries_the_platform_s_executable_suffix`, `a_clean_exit_is_an_install_only_when_the_search_then_finds_a_satz`, `an_update_by_the_installer_is_done_only_when_the_satz_in_use_is_that_release`, `the_log_header_names_this_system_s_installer_and_the_folder`, `the_installer_is_offered_unless_settings_name_a_satz` |
| `McpSession` opens the estate and types a tool call; `satz_transpile` under `--allow read` is `is_error` naming `write`; a refused `satz_open` is an error, not a session; a child that will not start is an error carrying what it said, and one that exits is `Closed` on the next call | `tests/satz_mcp.rs` | all |
| `EstateSession` over a copy of the smoke estate reads the identity and types a tool call; its root is the `--root` `satz mcp-config` renders for the estate — the estate's directory — and a writing tool (`satz_update_prerequisites` with `report_only: false`) runs on it, the session being started at `Allow::STUDIO`, `read,write`; the one-shot script changes into the estate and quotes what needs it | `tests/satz_session.rs` | all |
| The script for the terminal passes every path as written: single-quoted for `sh`, so a `$`, a backtick, a backslash or a quote of either kind in it is not expanded — checked by running it — and double-quoted with `%` doubled for `cmd.exe`; the agent's script starts in a directory whose name holds all of those | `src/satz/session.rs`, `src/agent.rs` | `the_script_changes_into_the_estate_and_quotes_what_needs_it`, `a_path_the_shell_would_expand_is_passed_as_written`, `a_percent_sign_is_doubled_for_a_batch_file`, `a_directory_the_shell_would_expand_is_the_one_the_agent_starts_in` |
| Against the installed satz, an argument satz cannot read is a refusal with `is_error` and a sentence, and a tool name it does not serve is `SatzError::InvalidParams` naming the tool — neither is a transport failure | `tests/satz_mcp.rs` | `a_bad_argument_is_a_refusal_and_an_unknown_tool_is_invalid_params` |
| The export picker is exactly satz's set: the formats read from the installed satz's `satz questions --help` equal the `[possible values: …]` satz names when it refuses a format, and include `markdown` and `xlsx`. Every one of them, exported from the smoke estate to a temporary directory under a name without an extension, writes a non-empty file at the name with the format's extension — the markdown sheet opening with its `# Decisions` heading, the workbook a zip (`PK`), the PDF a `%PDF` | `tests/e2e_export.rs` | `the_picker_offers_exactly_the_formats_satz_accepts`, `every_format_writes_its_document_at_the_named_path` |
| `--format`'s possible values are read from a clap long help in satz's order with satz's words, a wrapped line joined back; a help without the block, or with an empty one, is an error and never an empty list. The export is `questions <estate> --format <format> --out <file>` per format and nothing else; the destination always carries an extension, a chosen one kept; a missing or empty file after a clean exit is a failure | `src/satz/export.rs` | `the_possible_values_are_read_in_satz_s_order_with_satz_s_words`, `a_wrapped_description_is_joined_back_into_one`, `a_help_without_the_block_is_an_error_and_never_an_empty_list`, `the_export_is_the_cli_s_two_flags_per_format`, `the_destination_always_names_an_extension`, `a_missing_or_empty_file_is_a_failure` |
| The export card starts on the decisions sheet, `markdown`, when satz offers it, and on no format when it does not | `crates/satz-studio/src/views/export.rs` | `the_card_starts_on_the_decisions_sheet_when_satz_offers_it` |
| The configuration a client reads is one `satz mcp-config` run per button: `mcp-config <estate> --client <client> --allow <ceiling>` for the block on screen, the same with `--write` for "Configure <client>", and with `--write --force` only for the refusal satz answers that way; the ceiling Settings hold is written out on every run | `src/satz/mcp_config.rs` | `each_button_is_one_satz_run_over_the_open_estate`, `the_ceiling_is_always_passed_and_force_only_on_a_replace` |
| `satz mcp-config` end to end with the installed satz over a copy of the smoke estate: the block it prints for each client holds one key — `satz` for Claude Code, `satz-smoke` for Claude Desktop — the satz that printed it by absolute path, the estate's own directory as `--root` and the ceiling asked for, with its notes on stderr and no file written; a `--write` writes the file and says `wrote`, the same run again says `unchanged`, and the file satz's notes name is read back as holding that entry, and as another entry against the block for a lower ceiling; a lower ceiling is refused with the sentence that names `--force` and the file stands, and the `--write --force` run replaces it, after which the file holds the lower ceiling | `tests/e2e_mcp_config.rs` | `the_block_satz_prints_is_this_estates_own_server`, `a_write_lands_then_is_unchanged_then_refuses_a_differing_ceiling` |
| The block satz prints is read as its one server — key, command, `--root`, `--allow` — and a block of another shape (not JSON, no `mcpServers`, no server or two, no `command`) is `SatzError::Printed`, as is one without a `--root`; the file a write goes to is the one satz's notes name, for either client, and notes naming none are an error; the client's file is read for satz's key against the block: absent (no file, or another server only), the same entry, another entry — a lower ceiling, or none — and unreadable (not JSON, `mcpServers` not an object) | `src/satz/mcp_config.rs` | `the_block_is_read_as_one_server_with_its_root_and_ceiling`, `a_block_satz_does_not_print_is_refused`, `the_file_a_write_goes_to_is_the_one_satz_names`, `the_file_on_disk_is_compared_with_the_block` |
| What satz refused with is what the operator reads: the refusal is its stderr, the same bytes; a run that never spoke says what the app knows instead of nothing; the line a write ended on is the toast; and `--force` is offered only for satz's own sentence about a key that is there with other arguments, never for a file satz cannot parse | `src/satz/mcp_config.rs` | `a_refusal_is_shown_as_satz_wrote_it`, `a_run_that_never_spoke_says_what_the_app_knows`, `the_toast_carries_the_line_the_write_ended_on`, `a_differing_key_is_the_one_refusal_force_answers` |
| The agent command is a command line whose first word is the program, a quoted first word kept whole; a line with nothing in it is "no command configured" and a program that is not on `PATH` is refused by name — neither starts anything — and the script changes into the estate's directory before it runs | `src/agent.rs`, `crates/satz-studio/src/views/settings.rs` | `the_first_word_is_the_program_and_an_empty_line_names_none`, `a_command_that_is_not_configured_and_one_that_is_not_installed_say_so`, `the_script_changes_into_the_estate_directory_and_runs_the_command`, `a_command_that_is_installed_is_its_path_and_one_that_is_not_is_a_fault`, `a_field_with_nothing_in_it_says_there_is_no_client_to_start` |
| The settings file holds six fields and no credential: the defaults round-trip, a fresh file names `claude` as the agent command, a written one is read back, a partial file takes the defaults for the rest, a missing file is the first run and a broken one is an error | `src/settings.rs` | `defaults_round_trip_through_toml`, `a_fresh_settings_file_names_the_default_agent_and_a_written_one_is_read_back`, `a_partial_file_takes_defaults_for_the_rest`, `a_missing_file_is_the_first_run`, `a_broken_file_is_an_error` |
| The recorded `satz questions --format json` of the pinned satz round-trips through the report types | `src/satz/reports.rs` | `the_recorded_questions_report_round_trips` |
| The recorded `satz_merge_presets {report_only: true}` of the smoke estate — over a pristine library that added a file, moved a used pack and touched an unused one's comments — round-trips through `MergeReport`; the log reads it as sentences, one per pack it would change and the counts, with no line opening a JSON object or array; a notice it opened names its command; an event kind the app does not read fails the report | `src/satz/reports.rs` | `the_recorded_merge_report_round_trips`, `a_merge_reads_as_sentences_in_the_log`, `a_notice_a_merge_opened_names_the_command_to_run`, `a_merge_event_of_a_kind_the_app_does_not_read_fails_the_report` |
| A merge whose report says `attention` is a failed outcome in the log, as `satz merge-presets` exits non-zero on it; without it the outcome is ok | `crates/satz-studio/src/state/estate_actions.rs` | `a_merge_that_needs_attention_is_a_failed_outcome` |
| The one-click commands — `whoami` (offline here), `transpile --check` and `questions --format text` on the smoke estate — print satz's prose into the log, no line opening a JSON object or array | `tests/satz_cli.rs` | `the_one_click_commands_print_prose` |
| No palette entry opens on `json` or fixes a format in its fixed words; `update-prerequisites` runs `--report-only` alone; the one-click entries run satz's own commands at their defaults, `questions` in `text` into the app's file | `crates/satz-studio/src/views/commands.rs` | `no_entry_defaults_to_json_and_no_tail_fixes_a_format`, `every_reporting_command_names_a_format_and_a_destination`, `one_click_runs_satz_s_own_commands_in_text` |
| The recorded `satz packs --format json` of satz's smoke estate round-trips through `PacksReport`; a row without a field satz always sends, or with a line state satz does not write, fails; the pack switches send only what is set | `src/satz/reports.rs` | `the_recorded_packs_report_round_trips`, `a_pack_row_without_a_field_satz_always_sends_fails`, `pack_switch_args_send_only_what_is_set` |
| A reporting call writes one file, reads it back as a typed report and leaves neither the file nor its directory behind, the estate untouched | `tests/satz_cli.rs` | `a_reporting_call_leaves_nothing_behind` |
| Two recorded `satz review-pack --format json` of the pinned satz round-trip through `PackReview` — a library pack that clears the bar, and `tests/fixtures/review/team-access.satz`, which does not: three errors, the membership among them at its block and the layout with `satz fmt` as its fix; a review without a field satz always sends fails. Each finding is a diagnostic in the pack's file at the line satz named, from `satz review-pack`; the toast says the verdict with the errors or the warnings it counted | `src/satz/reports.rs`, `src/satz/review.rs`, `crates/satz-studio/src/state/estate_actions.rs` | `the_recorded_reviews_round_trip`, `a_review_passes_when_no_finding_is_an_error`, `a_review_without_a_field_satz_always_sends_fails`, `the_findings_are_diagnostics_at_file_and_line`, `the_arguments_name_the_pack_and_the_estate_it_is_judged_in`, `a_review_toast_says_the_verdict_and_what_it_counted` |
| The private destination: a pack is `<stem>.local.satz` in the library, a `.local.satz` keeps its name, a `.diff.satz` and a file that is not `.satz` are refused, and upstream it is `presets/<stem>.satz`. Placed, the file holds the reviewed bytes and the estate was checked, and the placed file reviewed where it stands is the library's own; placed again with the same bytes nothing is written or checked; a file of that name holding other text is refused and left as it is; a pack edited since its review is refused; a check that refuses removes the file; an estate without a library is refused | `src/satz/review.rs` | `a_private_pack_is_named_local_and_a_fork_keeps_its_name`, `upstream_the_pack_is_a_pristine_file_of_the_library`, `a_placed_pack_is_the_reviewed_bytes_under_its_local_name`, `a_fork_that_holds_other_text_is_refused_and_left_as_it_is`, `a_pack_edited_since_its_review_is_refused`, `a_placement_the_estate_refuses_is_removed_again`, `an_estate_without_a_library_has_nowhere_to_put_a_pack` |
| `satz review-pack` end to end with the installed satz, through the CLI with the estate's config as the Packs view runs it: `vendor/satz/presets/organization-budget.satz` clears the bar folded into the example values and emits; the broken fixture does not, the review holds the text it judged, every diagnostic names the pack, and the membership's line is the membership's own block in that text. Over an estate the smoke matrix's answers complete, the reviewed pack is placed in a scratch library as `team-access.local.satz` with the estate checked over the session by `McpChecker`; placed again it is already there; over a fork holding other text it is refused and the fork keeps its bytes | `tests/e2e_review.rs` | `a_library_pack_clears_the_bar_and_a_broken_one_is_findings_at_its_lines`, `a_reviewed_pack_is_placed_once_and_never_over_other_text` |
| A click in the drawer opens where the line is: a line of the main file in Estate, a finding about the reviewed pack in Packs — with a line or about the whole pack — and anything else nowhere; the Packs view marks the selected finding's line only when it is about the reviewed pack | `crates/satz-studio/src/shell/drawer.rs`, `crates/satz-studio/src/views/review.rs` | `a_line_of_the_main_file_is_in_estate_and_one_of_the_reviewed_pack_in_packs`, `the_marked_line_is_the_selected_finding_about_this_pack` |
| The recorded `structuredContent` of a refused `satz_transpile_check` reads as a `Refusal` and as a `CompileSummary`; a summary without `findings` reads, and a kind satz adds is carried | `src/satz/reports.rs` | `a_refusals_structured_content_reads_as_findings`, `a_summary_without_findings_reads_and_a_kind_satz_adds_is_carried` |
| A finding becomes a diagnostic: severity mapped, `kind` carried, a relative file resolved against the estate's directory, the group's header in front of the message with one colon | `src/diag.rs` | `a_finding_keeps_its_kind_and_resolves_its_file_against_the_estate`, `a_group_heads_the_message_and_keeps_one_colon`, `a_note_without_a_group_keeps_its_message_verbatim` |
| The CLI check's `--format json` summary is the verdict: a refusal is one diagnostic per finding, a relative file resolved against the estate's directory; a pass is the summary, and a zero exit whose stdout is not one is a failure, never a pass; a refusal before the compile is what stderr said, and never an empty list; a refused tool result reads its findings from `structuredContent`, falls to its text without them, and is an error on a payload of another shape | `src/edit/check.rs` | `a_refusal_is_one_diagnostic_per_finding_resolved_against_the_estate_directory`, `a_pass_is_the_summary_and_a_pass_that_is_not_data_is_a_failure`, `a_failure_before_the_compile_is_what_stderr_said`, `the_mcp_refusal_reads_its_structured_findings_and_falls_to_the_text_without_them` |
| satz's laid-out findings are one diagnostic per row: a block's rows share the message under them, the group's title stands in front of it, the title and the footer are no diagnostics, and a row without a location says its subject; a line that only starts with a severity word is no row | `src/diag.rs` | `the_laid_out_findings_are_one_diagnostic_per_row`, `a_line_that_only_starts_with_a_severity_word_is_no_row` |
| A command refused because another is running leaves the running command's cancel token in place, so Cancel still stops it; the next command that starts is the one Cancel reaches | `crates/satz-studio/src/state/estate_actions.rs` | `a_refused_second_command_leaves_the_first_one_cancellable`, `the_next_command_is_the_one_cancel_reaches` |

## 2. The manual smoke walk

The estate views are checked by hand: [`ui.md`](ui.md), section "The smoke walk" —
sixteen steps over a copy of the smoke estate, a skeleton and a Terraform directory; no
credential is needed, step 12 needs an agentic client installed, and step 13 needs a
script in front of the installed satz that answers `--version` with a newer one. It is
the check of the window over what the harness proves of the core.

## 3. Checks that need a machine

- **The client itself.** With Claude Code installed, the Agent destination's "Configure
  Claude Code" and then "Open in claude": `/mcp` in the terminal that opens lists the
  satz server, and `satz_questions` there answers for the estate the window has open.
  What the harness proves is everything up to that terminal — the run each button makes,
  the block satz writes and the refusals — and what needs a machine is the client
  reading it.
- **The bundles.** `.github/workflows/release.yml` builds the `.app` and `.dmg` on
  `macos-15` (arm64 — macOS is Apple silicon alone), the `.deb` and `.AppImage` on
  `ubuntu-24.04` and the `.msi` on `windows-2022`, both x86_64. Every bundle carries
  `LICENSE`, `NOTICE`, `THIRD-PARTY-LICENSES.md` (the licence text of every crate the app is compiled from), `LICENSE-MaterialSymbols` (the icon font's) and `LICENSE-satz-tree-sitter` (the vendored grammar's) (`bundle.resources` in `crates/satz-studio/Dioxus.toml`): in
  `SatzStudio.app/Contents/Resources/`, in the MSI's install directory beside the
  executable, and in `/usr/lib/SatzStudio/` in the `.deb` and the AppImage. The macOS job
  compares the five files in the `.app` with the checkout's and fails without them,
  because dx skips a resource it cannot find with a warning and exits 0; the `.deb`, the
  AppImage and the MSI are opened by hand after a tag to see them. A local
  `dx bundle` runs from the repository root, where dx finds the five paths. That each bundle launches and opens
  the fixture estate is checked by hand on a machine of that OS: unzip or mount, start
  the app past the unsigned-build prompt the README names, the Open door → the
  repository's `tests/fixtures` → Open → the top bar shows `smoke.satz`, and the window
  lands on the Overview with the estate's own facts in it and "the ADC identity — local
  mode" in its Runs as row. On Linux the `.deb` declares the runtime libraries; the AppImage
  needs `webkit2gtk-4.1` on the host. On Windows satz
  releases `satz-x86_64-pc-windows-msvc.zip` with `satz-installer.ps1` beside it, which
  installs that build on ARM64 Windows too; with no satz, the banner's "Install satz"
  runs that installer. That run is checked by hand on a Windows machine without satz:
  the log names `satz-installer.ps1` as verified, `satz.exe` lands in
  `%USERPROFILE%\.local\bin`, the user `PATH` is unchanged, and the banner goes.

## 4. How to run everything

```sh
cargo fmt -p satz-studio-core -p satz-studio -- --check      # the two packages
cargo clippy --workspace --all-targets --locked -- -D warnings
cargo test --workspace --locked                                # every unit and integration test
bash scripts/e2e.sh                                            # the satz gate, the e2e tests, a build of the app
bash scripts/check-names.sh                                    # the privacy gate over the tree
bash scripts/update-third-party-licenses.sh --check            # THIRD-PARTY-LICENSES.md against Cargo.lock (cargo-about 0.9.2, after cargo fetch --locked)
dx bundle --package satz-studio --platform desktop --release   # this machine's bundle, under target/dx/satz-studio/bundle/
```

`scripts/e2e.sh` checks that `satz --version` is at least `MIN_SATZ`
(`crates/satz-studio-core/src/satz/binary.rs`), runs
`cargo test -p satz-studio-core --locked --test 'e2e_*'`, and without `--ci` also
`cargo build -p satz-studio --locked`; one verdict line per step, and the first failure
ends the run. `ci.yml` runs it with `--ci` after the tests on every pull request and on
every push to `main`. The e2e tests each write a temporary estate whose `config.toml` points into
`vendor/satz` by absolute path, so nothing under the repository is written.
